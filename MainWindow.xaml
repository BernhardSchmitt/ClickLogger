<Window x:Class="ClickLogger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ClickLogger.ViewModel"  
        Title="Click Logger"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" MouseLeftButtonDown="Window_MouseDown">
    
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    <Window.Resources>
        <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="PulseEffect" Storyboard.TargetProperty="Opacity" From="1.0" To="0.0" Duration="0:0:1" AutoReverse="True"/>
            <DoubleAnimation Storyboard.TargetName="PulseScale" Storyboard.TargetProperty="ScaleX" From="1.0" To="1.2" Duration="0:0:1"/>
            <DoubleAnimation Storyboard.TargetName="PulseScale" Storyboard.TargetProperty="ScaleY" From="1.0" To="1.2" Duration="0:0:1"/>
        </Storyboard>
        
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Width" Value="18"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="{TemplateBinding Background}">
                            <TextBlock Text="{TemplateBinding Content}" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF3333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="HoverTextStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,4,0,0"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            
                            <ContentPresenter 
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#FFFF66"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#444444" 
            BorderThickness="1" 
            CornerRadius="6"
            Background="#AA222222"
            Padding="4">
        
        <Grid>
            <StackPanel VerticalAlignment="Center" 
                        HorizontalAlignment="Center" 
                        Margin="0,18,0,0"> 

                <Grid Margin="0,0,0,6">
                    <Ellipse x:Name="PulseEffect" Fill="#FF4444" Width="44" Height="44" Opacity="0">
                        <Ellipse.RenderTransform>
                            <ScaleTransform x:Name="PulseScale" CenterX="22" CenterY="22"/>
                        </Ellipse.RenderTransform>
                    </Ellipse>

                    <Button x:Name="RecordBtn" 
                            Command="{Binding ToggleRecordCommand}" 
                            Width="44" Height="44" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Grid>
                                    <Ellipse x:Name="Bg" Fill="#333" Stroke="White" StrokeThickness="1"/>
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>
                            </ControlTemplate>
                        </Button.Template>
                        <TextBlock x:Name="BtnText" 
                                   Text="{Binding RecordButtonText}" 
                                   Foreground="White" FontWeight="Bold" FontSize="10"/>
                    </Button>
                </Grid>
                
                <TextBlock x:Name="FileNameLabel" 
                           Text="{Binding FileName}" 
                           ToolTip="{Binding FolderPath}"
                           Foreground="White" FontSize="8" HorizontalAlignment="Center" 
                           Margin="0,4,0,0" TextTrimming="CharacterEllipsis" Width="120"/>

                <Button Command="{Binding OpenInExplorerCommand}" 
                        Style="{StaticResource HoverTextStyle}"
                        ToolTip="{Binding FolderPath}">
                    <TextBlock Text="Open Log Folder" />
                </Button>
            </StackPanel>

            <Button Content="X" 
                    Command="{Binding QuitCommand}" 
                    Style="{StaticResource CloseButtonStyle}"
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Top" 
                    ToolTip="Quit ClickLogger"/>
        </Grid>
    </Border>
</Window>